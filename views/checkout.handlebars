<html>
  <head>
    <meta charset="utf-8">
    <title>Butternut Squishies Grocery Store</title>
    <link rel="stylesheet" href="/static/style.css">
    <link rel="stylesheet" href="/static/admin.css">
    <link href='https://fonts.googleapis.com/css?family=Calligraffitti' rel='stylesheet'>
  </head>
  <body>
    <h1 class ="storeTitle" >Butternut Squishies Grocery Store</h1>
      <li class="registerButtonSection" ><a class="register" href="register">Register</a></li>
    <ul class="nav">
        <li><a class="tab" href="/">Home</a></li>
        <li><a class="tab" href="/departments">Departments</a></li>
        <li><a class="tab" href="accounts">Account</a></li>
        <li><a class="tab" href="admin">Admin</a></li>
        <li><a class ="active checkoutBox" href="checkout">Check Out</a></li>
    </ul>

   <h2 class="title1">Cart</h2>
   <form class="cart formCart">
    <table class="cart">
      <thead>
        <tr>
          <th> Name </th>
          <th> Quantity </th>
          <th> Unit Price </th>
          <th> Discount</th>
          <th> Subtotal </th>
          <th> Update </th>
          <th> Delete </th>
        </tr>
      </thead>
       {{#each orderDetail}}
        <tr>
          <td>{{name}}</td>
          <td>{{quantity}}</td>
          <td>{{unit_price}}</td>
          <td>{{discount}}</td>
          <td>{{subtotal}}</td>
          <td>
            <form>
                <input type ="hidden" value =
              <input type="button" value="update">
            </form>
          </td>
          <td>
            <form>
              <input type="button" value="delete">
            </form></td>
        </tr>
        {{/each}}
    </table>

      <h2 class="cart">{{total_price}}</h2>

      <br>
      <label> {{note}}</label><br>
      <textarea rows="4" cols="50" name="comment" form="usrform">
      </textarea><br>
      <input class="cart" type="submit" value="checkOut" >
      </form>
      <h3></h3>
      <p></p>

  </body>
</html>
