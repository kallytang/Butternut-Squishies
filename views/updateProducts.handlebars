<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Butternut Squishies Grocery Store</title>
    <link rel="stylesheet" href="/static/style.css">
    <link rel="stylesheet" href="/static/admin.css">
    <link href='https://fonts.googleapis.com/css?family=Calligraffitti' rel='stylesheet'>
  </head>
  <body>
    <h2>Update Products</h2>

 <table>

      <thead>
        <tr>
          <th> Product Id </th>
          <th> Name </th>
          <th> Description </th>
          <th> Department </th>
          <th> Price </th>
          <th> Quantity/Stock </th>
          <th> Sale </th>
          <th> Image Path </th>
          <th> Update </th>
        </tr>
      </thead>

        <tr>
            <td>{{product.product_id}}</td>
            <td><input type="text" max = "255" name="prodName" value ={{product.name}}></td>
            <td><input type="text" max = "255" name="prodDesc" value ={{product.description}}></td>
            <td>
                {{#each departments}}
                <input type="radio" max = "255" name="deptID" value ={{{department_id}}}> {{{name}}}
                {{/each}}
            </td>
            <td><input type="number" min="0.00" step= "0.01" max="1000.00" name="prodPrice" value ={{product.price}}></td>
            <td><input type="number" min="0" max = "10000" name="prodStock" value ={{product.stock}}></td>
            <td>
                <input type="radio" max = "255" name="prodSale" value ="0" >Not on Sale <br>
                <input type="radio" max = "255" name="prodSale" value ="1">Sale <br>
            </td>
          <td><input type="text" max = "255" name="prodImage" value ={{product.image_path}} ></td>
            <td>
                <form action="/updateProducts" method="post">
                <input type="hidden" name= "prodID" value={{product.product_id}}>
                <input class="button" type="submit" value="Update">
                </form>
            </td>
        </tr>

    </table>
  </body>
</html>
